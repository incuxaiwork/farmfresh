# Project Rules - FarmFresh

## Git Operations
- Do NOT automatically push commits to the remote repository. 
- Only push commits to remote repositories (GitHub/GitLab/etc.) when the USER has given explicit permission in the conversation chat.
